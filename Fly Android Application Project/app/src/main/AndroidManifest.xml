<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_app_logo"
        android:label="@string/str_app_name"
        android:roundIcon="@mipmap/ic_app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fly"
        tools:targetApi="31">
        <activity
            android:name="activities.SavedFlightsActivity"
            android:parentActivityName="activities.MainActivity"
            android:exported="true" />
        <activity
            android:name="activities.ReturnFlightsResultsActivity"
            android:parentActivityName="activities.DepartureFlightsResultsActivity"
            android:exported="true" />
        <activity
            android:name="activities.SearchFlightsActivity"
            android:parentActivityName="activities.MainActivity"
            android:exported="true" />
        <activity
            android:name="activities.DepartureFlightsResultsActivity"
            android:parentActivityName="activities.SearchFlightsActivity"
            android:exported="true" />
        <activity
            android:name="activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>